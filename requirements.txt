# Server (cloud) edition runtime deps.
#
# The shared spine (Django, channels, psycopg v3, redis, django-redis, requests,
# cryptography, whitenoise, corsheaders, anthropic, google-genai, tzdata, ...) is
# installed from the alpha_pos_core git SUBMODULE in the Dockerfile
# (`pip install ./alpha_pos_core`). This file is ONLY the server-edition extras
# layered on top. The first checkout MUST be `git clone --recurse-submodules`
# (deploy.sh also runs `git submodule update --init` defensively).
channels-redis==4.3.0        # Redis channel layer: websocket group fan-out across uvicorn workers
uvicorn[standard]==0.49.0    # ASGI server (replaces gunicorn / WSGI)
openpyxl==3.1.5              # Native XLSX dashboard and shift-report exports
